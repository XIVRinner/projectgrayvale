# Shared Layer

This folder owns pure reusable UI pieces:
- Presentational components
- Directives and pipes
- Theme tokens and PrimeNG theme preset

Shared components should not access store or data services.
Shared UI should prefer progressive disclosure for explanations: keep default layouts compact, and move definitions or longer guidance into tooltips, hover states, or similar secondary affordances.
